# hyprlang noerror false
# ccsmith33 overrides — overgrowth aesthetic

general {
    # Thicker borders so active window is obvious
    border_size = 3
    gaps_in = 4
    gaps_out = 8

    # Solid moss-mid for active (high visibility), dark bark for inactive
    col.active_border = rgba(6c8a59ff)
    col.inactive_border = rgba(2a241cff)
}
